#include <bits/stdc++.h>
#include <time.h>  
#define FAST ios_base::sync_with_stdio(false); cin.tie(NULL);
#define ll long long
#define rep(i,a,b) for(ll i=a;i<b;++i) 
#define multi_case int t; std::cin>>t; while(t--) slv();
using namespace std;
const int N=1e6+7;
ll f[N],F[N];
void slv(){
	FILE *fp;
	fp=fopen("test1.txt","w");
	int t,n; cin>>t;
	fprintf(fp,"%d\n",t);
	srand(time(0));
	rep(i,0,t){
		rep(j,0,t+1)
			fprintf(fp,"%d ",rand()%100);
		fprintf(fp,"\n");
	}
}
main(){
	FAST;
	//multi_case;
	slv();
}
